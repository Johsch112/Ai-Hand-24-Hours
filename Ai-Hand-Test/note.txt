Fick koden från https://github.com/Creativeguru97/YouTube_tutorial/tree/master/Play_with_APIs/hand_detection/mediapipe_hands/final

Vad Jag och Jason har gjort nu är att lägga till så den känner av olika hand rörelser.
En viss handrörelse triggar att skärmen blir grön.

Planen nu är att ändra på denna sektion i sketch.js

function checknear() //check if the Z indexes are near eachother.
{
    let z1 = detections[0].annotations.indexFinger[0][2];
    let z2 = detections[0].annotations.indexFinger[1][2];
    let z3 = detections[0].annotations.indexFinger[2][2];
    let z4 = detections[0].annotations.indexFinger[3][2];
    if(z1 - z2 <= 1 & z1 - z3 <= 1 & z2 - z4 <= 1)
    {
        console.log("BANG!");
        game.classList.add("bang")
    } else {
        game.classList.remove("bang")

    }
}

Just nu beräknar den endast utifrån z. planen är att, på något sätt, lägga till
kontroller för x och y också. Tänkte bara skriva detta ifall någon känner sig mån på att
programmera lite extra. Ha det bra!

--Philip